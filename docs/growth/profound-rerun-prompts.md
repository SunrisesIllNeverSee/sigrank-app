# SigRank — Profound AEO Re-run Prompt Set (30 prompts)

Operator-based metrics weighted to the top per request. Keep this EXACT set on every
re-run so before/after is comparable. Override Profound's auto-picked competitors.

## Competitors to set (replace YouGov/Kantar/Qualtrics)
- ccusage              (primary — same npm/CLI niche)
- tokscale             (primary)
- tokendash            (primary)
- LMArena / Chatbot Arena   (category neighbor)
- OpenRouter                (category neighbor)

## Topics / keywords
AI agent leaderboard | LLM token efficiency | token usage analytics |
prompt caching efficiency | MCP tools | AI agent benchmarking | operator metrics

---

## Prompts (flat — paste one per line)

# --- Operator-based metrics (priority) ---
how do you measure the efficiency of an AI coding agent
what metrics rank AI operators by token efficiency
how to score an AI agent by output per token
how to compare AI operators by cache-read efficiency
best metric for LLM token cascade efficiency
how to rank AI agents by cost and output quality
how to track which AI operators are most token-efficient
how is AI operator performance scored over 7 / 30 / 90 day windows
how do I prove my AI agent is efficient
how to get my AI agent ranked on a public leaderboard

# --- Category / discovery (unbranded) ---
best AI agent leaderboards
leaderboards that rank AI coding agents
is there a leaderboard for LLM token efficiency
tools to benchmark AI agent performance
MCP servers for tracking AI agent usage
open leaderboards for AI operators
privacy-preserving way to share AI usage metrics

# --- Comparison ---
SigRank vs ccusage
ccusage vs tokscale vs SigRank
best alternatives to ccusage for token analytics
token usage CLI tools compared
SigRank vs Chatbot Arena for ranking agents

# --- Branded / entity-verification (tests the JSON-LD + llms.txt fixes) ---
what is SigRank
what does SigRank measure
how does SigRank rank operators
how do I enroll as an operator on SigRank
does SigRank read my prompts
what tools does the SigRank MCP server provide

# --- Definitional (maps to DefinedTerm markup) ---
what is token cascade efficiency
what is the yield (Υ) metric in AI agent scoring

---

## How to read the re-run
- Branded + definitional tiers move FIRST if the structured-data fixes landed —
  watch "not mentioned" -> "mentioned/understood". That's the fix working.
- Unbranded category moves SLOWLY; needs off-site footprint (blog, Reddit,
  MCP directories), not on-site markup alone. Don't expect Citation Share to
  jump from JSON-LD by itself.
- Ignore Profound's "Signal Quality / Interference" content suggestions — those
  came from the wrong (market-research / signal-processing) category guess.
